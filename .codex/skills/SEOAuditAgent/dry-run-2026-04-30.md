# SEOAuditAgent Dry Run — 2026-04-30

This file captures the first live dry run against real `/admin/seo-insights` findings.

## Status

Executed manually on `2026-05-02`.

## Execution — 2026-05-02

- Week reviewed: `2026-04-22`
- Findings sampled per bucket:
  - `winnable_loss`: `0`
  - `content_gap`: `1`
  - `trend_signal`: `0`
  - `decay`: `0`
- Packaging audits reviewed: `3`
- Scored portfolio rows reviewed: `2`
- Measured actions: `0`

## Lane Decisions

- `content_gap` — query `"turing award" "multiple winners" quiz` on `/news`
  - Lane: `propose`
  - Result: `POST /api/admin/seo/insights/:id/generate-proposal` returned `200`
- Packaging `/news/cmjdkzts4001i02i8du50k55m`
  - Lane: `propose`
  - Result: `POST /api/admin/seo/packaging/:id/propose-fix` returned `409` because a packaging fix was already queued recently
- Packaging `/news`
  - Lane: `propose`
  - Result: `POST /api/admin/seo/packaging/:id/propose-fix` returned `409` because a packaging fix was already queued recently
- Packaging `/`
  - Lane: `human_only`
  - Reason: homepage title/H1 alignment is too brand-sensitive for an automated packaging proposal
- Portfolio `ai timeline`
  - Lane: `human_only`
  - Reason: scored `45.4`, below the weekly discovery-promotion threshold of `60`
- Portfolio `turing award multiple winners`
  - Lane: `human_only`
  - Reason: scored `43.4`, below the weekly discovery-promotion threshold of `60`

## Artifacts Generated

- `1` new proposal from the live content-gap finding
- persisted `PUT /api/admin/seo/run-status` record with:
  - `proposalCount=1`
  - `humanOnlyCount=3`
  - captured Serper snapshot (`4` weekly queries, `$0.0040` month-to-date modeled spend, tracked vendor balance `2,496`)

## Reviews And Follow-up

- `/AISEOReview` verdicts: not run during this first manual digest
- Wylie feedback: pending
- Playbook or slop-rule changes: none yet; the first manual run did not require threshold or copy-policy changes
